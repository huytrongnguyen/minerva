{
  "sources": [{
    "type": "csv",
    "options": {
      "header": "true"
    },
    "preprocess": "shared/raw/appsflyer_backfill_preprocess.sql",
    "location": "{{sources.appsflyer.location}}/id123456789/install.csv"
  }],
  "targets": [{
    "type": "csv",
    "options": {
      "header": "true"
    },
    "partition_by": ["app_id","event_date"],
    "location": "{{targets.warehouse.location}}/{{product_id}}/appsflyer/installs_report"
  }]
}
{
  "sources": [{
    "name": "appsflyer_installs_report",
    "type": "csv",
    "location": "{{location(inputs,appsflyer)}}/{{vars.app_id}}/{{event_date}}/installs_report"
  }],
  "targets": [{
    "location": "{{location(datastore)}}/{{vars.product_id}}/devices/installs/{{event_date}}"
  }]
}